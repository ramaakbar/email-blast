node-linker=hoisted
# electron-builder packages either layout (it ships a pnpm collector); the
# hoisted tree is kept so the packaged app resolves its one external module
# (better-sqlite3) exactly as development does. ADR-0010.
