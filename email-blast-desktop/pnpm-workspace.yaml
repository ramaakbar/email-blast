allowBuilds:
  electron: true
  esbuild: true
  msgpackr-extract: false
  # Pulled in by electron-builder for its Squirrel.Windows target, which this
  # app does not build (the Windows target is NSIS): its install script only
  # copies a 7z binary nothing here runs.
  electron-winstaller: false
  better-sqlite3: true
