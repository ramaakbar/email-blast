import path from "path";

// === Konstanta path template ===
const TEMPLATE_DIR = "./templates";
export const TEMPLATE_LOLOS = path.resolve(TEMPLATE_DIR, "template-lolos.docx");
export const TEMPLATE_TIDAK = path.resolve(
  TEMPLATE_DIR,
  "template-tidak-lolos.docx"
);
